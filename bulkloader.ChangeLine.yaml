# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users
- import: loader

transformers:
- kind: ChangeLine
  connector: csv
  connector_options:
    column_list: [year, leading_item, req_code, req_title, change_code, change_title, change_type_id, change_type_name, committee_id, budget_code, budget_title, net_expense_diff, gross_expense_diff, allocated_income_diff, commitment_limit_diff, personnel_max_diff, date, explanation]
  property_map:
  - property: year
    external_name: year
    import_transform: int
  - property: leading_item
    external_name: leading_item
    import_transform: int
  - property: req_code
    external_name: req_code
    import_transform: int
  - property: req_title
    external_name: req_title
  - property: change_code
    external_name: change_code
    import_transform: int
  - property: change_title
    external_name: change_title
  - property: change_type_id
    external_name: change_type_id
    import_transform: int
  - property: change_type_name
    external_name: change_type_name
  - property: committee_id
    external_name: committee_id
    import_transform: int
  - property: budget_code
    external_name: budget_code
  - property: budget_title
    external_name: budget_title
  - property: net_expense_diff
    external_name: net_expense_diff
    import_transform: int
  - property: gross_expense_diff
    external_name: gross_expense_diff
    import_transform: int
  - property: allocated_income_diff
    external_name: allocated_income_diff
    import_transform: int
  - property: commitment_limit_diff
    external_name: commitment_limit_diff
    import_transform: int
  - property: personnel_max_diff
    external_name: personnel_max_diff
    import_transform: float
  - property: date
    external_name: date
    import_transform: transform.import_date_time('%d/%m/%Y')
  - property: explanation
    external_name: explanation
    import_transform: "lambda x : x[:500]"
  post_import_function: loader.load_change_line
